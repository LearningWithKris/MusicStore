@model MusicStore.Models.Genre

@{
    ViewBag.Title = "Browse";
}

<h2>Browse Genre: @Model.Name</h2>

<ul>
    @foreach (var album in Model.Albums)
    {
        <li>
             @Html.ActionLink(album.Title, "Details", new { ID = album.AlbumID })
        </li>
        
    }
</ul>

@*@model MusicStore.Models.Album

@{
    ViewBag.Title = "Browse";
}

<h2>Browse Genre: @Model.Title</h2>

<ul>
    @foreach (var genre in Model.Genre)
    {
        <li>
            @Html.ActionLink(genre.Title, "Details", new { ID = genre.GenreID })
        </li>

    }
</ul>*@